import {User} from '../models/user.js'
import mongoose from 'mongoose'

import { faker } from '@faker-js/faker'


// imput validation

// unique clients

//only admin can set other admins but default role is user
users can only see their sessions


//login page has no connection button

//can we create a user
//can the user login

//is the password hashed
//are the users unique
//is password == confirmation password

//NEEEED TO TEST DB CONNECTION BECAUSE  when its not connected, the state of the device ddnt change
